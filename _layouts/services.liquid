<div class="col mb-4">
  <div class="card">
    <div class="card-body">
      <h5 class="card-title">{{ services.title }}</h5>
      <p class="card-text">{{ services.category }}</p>
    </div>
  </div>
</div>
